[package]
name = "gc_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "gc_core"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module", "abi3-py312"] }
dbn = "0.61"
rustc-hash = "2"

[profile.release]
lto = true
codegen-units = 1
